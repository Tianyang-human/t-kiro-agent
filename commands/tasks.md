Use the tasks-agent to create a task breakdown for: $ARGUMENTS

The agent will read the requirements and design documents from `~/.kiro/<repo-name>/<feature-name>/` and write the task list to `~/.kiro/<repo-name>/<feature-name>/tasks.md`. All specs live outside the repository.
