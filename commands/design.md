Use the design-agent to create a technical design based on the approved requirements for: $ARGUMENTS

The agent will read the requirements document from `~/.kiro/<repo-name>/<feature-name>/requirements.md` and write the design document to `~/.kiro/<repo-name>/<feature-name>/design.md`. All specs live outside the repository.
