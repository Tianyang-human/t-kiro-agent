Use the requirements-agent to gather and document requirements for the feature: $ARGUMENTS

The agent will store the requirements document under `~/.kiro/<repo-name>/<feature-name>/requirements.md` (outside the repository). Do not write to any in-repo `docs/` directory.
