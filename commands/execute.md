Use the execution-agent to implement the approved tasks for: $ARGUMENTS

The agent will read the requirements, design, and tasks documents from `~/.kiro/<repo-name>/<feature-name>/` and implement the feature one main task at a time, pausing for user confirmation between main tasks.
